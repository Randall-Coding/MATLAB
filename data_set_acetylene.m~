function [] = data_set_acetylene()

    load acetylene.mat;

    disp('This is acetylene data');

    plot(x1,y);
    pause(1);
    figure;
    plot(x2,y);
    pause(1);
    figure;
    plot(x3,y);

    option = '';  

    while opt 
        option = input('press q to close graphs','s');
    end

   % close all;

end 